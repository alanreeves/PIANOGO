export const APP_VERSION = "0.2.4";

export const DEFAULT_SETTINGS = Object.freeze({
  startTempo: 60,
  tempoIncrement: 2,
  repetitions: 8,
  hands: "both",
  countInSignature: "auto",
  pianoSound: true,
  upperClef: "auto",
  lowerClef: "auto",
});
