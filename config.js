export const APP_VERSION = "0.6.3";

export const DEFAULT_SETTINGS = Object.freeze({
  startTempo: 60,
  tempoIncrement: 2,
  repetitions: 8,
  hands: "both",
  countInSignature: "auto",
  pianoSound: true,
  upperClef: "auto",
  lowerClef: "auto",
});
